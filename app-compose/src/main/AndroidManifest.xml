<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Scaffold: default Application (no android:name) so this slice isolates
         "Compose builds + launches" from core's native bootstrap. SP1-B switches
         android:name to aenu.ax360e.Application when the library needs the emulator.
         @drawable/app_icon is provided by :emulator-core; @string/app_name and the
         theme are app-compose's own. -->
    <application
        android:allowBackup="true"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ax360eCompose">
        <activity
            android:name="aenu.ax360e.compose.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
